# dextra — برومبت استئناف (المرحلة 11: المُحمِّل الذكي) — انسخه في محادثة جديدة

> الغرض: استئناف العمل على dextra من حيث انتهينا — **المرحلة 11.1 (المُحمِّل الذكي csv/tsv) مكتملة وخضراء بالكامل** — ثم متابعة المسار المتفق عليه.

---

## 0) دورك
أنت مطوّر/مراجع تقني للمكتبة. التزم: لا مجاملة، أصلِح المشكلات مباشرةً كشرائح خضراء صغيرة (كود + اختبارات + run_validation + commit)، وصنّف أي نقطة غير مؤكَّدة صراحةً (تحتاج تحقّق / غير مثبتة).

## 1) الحالة الحالية (الأساس)
- **المستودع:** `D:\06 PythonProjects\dextra-project` — GitHub `ahmedabdeltawab602-collab/dextra`، فرع `main`.
- **بيئة المالك:** Python 3.13.13، venv، pandas **3.0.3**، numpy 2.4.5، scikit-learn 1.8.0. التفعيل: `cd "D:\06 PythonProjects\dextra-project"; .\.venv\Scripts\Activate.ps1`
- **الأساس الأخضر الفعلي (مُتحقَّق على جهاز المالك):** `run_validation.ps1` → **ALL GREEN**: ruff نظيف، **309 اختباراً تمر**، تغطية إجمالية **76.04%** (العتبة 68%).
- **الإصدار:** `src/dextra/_version.py` = **0.2.0** (لم يُرفع بعد — بند مفتوح).

## 2) ما أُنجِز في الجلسة السابقة
1. **مراجعة مستقلة شاملة** للمكتبة كلها → `AUDIT_REPORT.md` و`AUDIT_REPORT.docx` (الحكم: «جاهزة بعد إصلاحات محدَّدة»؛ عام 82/معماري 85/برمجي 80/إنتاجي 66؛ 14 مشكلة بأولويات وتصحيحات).
2. **تثبيت تموضع المكتبة:** «أداة تحليل بيانات آمنة + قابلة للتدقيق، للتعليم والتقارير السريعة».
3. **وثيقة فلسفة المُحمِّل** `LOADER_PHILOSOPHY.md` + **مواصفة تنفيذ** `LOADER_SPEC_11_1.md`.
4. **تنفيذ المرحلة 11.1 (المُحمِّل الذكي csv/tsv) — مكتملة وخضراء:**
   - `src/dextra/_loader.py`: `load`/`peek` (+ aliases `dload`/`dpeek`).
   - `src/dextra/_utils.py`: أُضيفت helpers مشتركة `now_iso`/`append_audit`/`json_safe` (مصدر حقيقة واحد — بدء معالجة بند المراجعة #5).
   - تسجيل في `__init__.py` (+`__all__`، تسميات الأطوار: «Phase 11 - loader (entry layer)»).
   - `pyproject.toml`: extra جديد `io = [charset-normalizer, clevercsv]` (+ مضافة إلى `dev` ليختبرها CI).
   - `tests/test_phase11.py` (22 اختباراً).
   - تحديث `ROADMAP.md` و`CHANGELOG.md`.

## 3) القرارات المثبَّتة للمُحمِّل (لا تُعِد فتحها)
- **الشفافية تتناسب مع عدم اليقين:** ثقة عالية ← تحميل بسطر واحد + إفصاح كامل؛ غموض ← علّم واطلب تأكيداً.
- «اطلب تأكيداً» = نمط **اقترح←افحص←طبّق غير حاجب** (لا `input()` افتراضاً؛ `interactive=True` للعرض الحيّ فقط).
- سياسة `on_ambiguous`: **`warn` افتراضاً** / `raise` للتدقيق-CI / `plan` للتعليم (يعيد الخطة بلا تحميل).
- **ثقة فئوية + سبب** (`confirmed`/`ambiguous`/`ambiguous-high-risk`) — لا أرقام احتمالية زائفة.
- **خطة التحميل = أثر `params` الموحّد** القابل للإعادة (مقروء + JSON + معدَّل + مبصوم بـsha256؛ `load(src, params=plan)` يعيد الإطار حتمياً، ويكتشف تغيّر المصدر).
- **الأمان الافتراضي:** رفض pickle تلقائياً (`allow_pickle=False`)، (وفي 11.3: SQL ببارامترات فقط).
- **الحدّ مع التنظيف (Phase 3):** المُحمِّل يحلّل ويُفصِح فقط (تحويلات قراءة آمنة قابلة للعكس)؛ التنظيف يُصلح القيم. جسر لا تداخل.
- **لا تُعِد الاختراع:** لفّ `charset-normalizer`/`clevercsv`/`pandas` كسولاً مع بدائل stdlib.
- `load` يلتزم العقد الموحّد كاملاً (النموذج الذهبي — معالجة اتجاه بند المراجعة #4).

## 4) تحذيرات بيئية مهمة (تعلّمناها بالتجربة)
1. **المزامنة تُفسد ملفات أدوات الكتابة (Write/Edit):** قطعت `_utils.py` و`__init__.py` فعلياً. **اكتب/عدّل ملفات الكود عبر bash ثم تحقّق فوراً** (`py_compile` + عدّ null bytes). لا تثق بنجاح Write/Edit دون تحقّق.
2. **بيئة المساعد المعزولة:** Python 3.10 + pandas 2.x فقط (بلا scipy/sklearn/statsmodels/streamlit/pytest/ruff). لذلك `import dextra` الكامل وpytest لا يعملان فيها؛ تُختبر وحدة `_loader` في عزلة عبر shim. **التحقّق الكامل دائماً على جهاز المالك** عبر `run_validation.ps1`.
3. **فروق pandas 3.0:** أعمدة النصوص تصبح `StringDtype` لا `object` (فاجأتنا مرتين). استخدم `pd.api.types.is_object_dtype/is_string_dtype` لا `== object` (ruff E721 يرفض الأخيرة).

## 5) المعلّق (لم يُنفَّذ بعد)
- **commit للمرحلة 11.1** (الكود أخضر لكن لم يُثبَّت بعد — انظر §7).
- **بنود المراجعة السريعة المحرِّرة لـ«جاهزة للنشر»:** #1 رفع الإصدار إلى 0.3.0 + CHANGELOG؛ #2 `git rm src/dextra/stats_advanced.py.bak` (متتبَّع ويُشحن)؛ #3 جعل صيغة بيانات اللوحة الافتراضية parquet/csv بدل pickle + تحذير أمني.
- **قيد تجميلي معروف في 11.1:** فحص استقرار الفاصل بتقسيم ساذج يرفع «delimiter ambiguous» أحياناً على ملفات بها تمهيد/اقتباسات (تحذير لا خطأ) — يُحسّن لاحقاً بإجراء الفحص بعد الترويسة.
- بنود CI من المراجعة: وظيفة بـ`[perf]` (parquet/polars) ووظيفة بمصفوفة إصدارات pandas/numpy (#8/#8b).

## 6) المطلوب في هذه الجلسة (اختر مع المالك)
- **المسار A (موصى به أولاً):** شريحة «إصلاحات الإصدار» (#1+#2+#3) — صغيرة وتحرّر «جاهزة للنشر».
- **المسار B:** **المرحلة 11.2 — Excel** (`load` لملفات xlsx/xls: سرد/اختيار الأوراق، كشف كتلة البيانات، ترويسات متعدّدة الصفوف، قراءة القيم لا الصيغ، تسلسلات التواريخ) عبر `openpyxl` كسولاً ضمن extra `io`. ثم 11.3 (SQL آمن + parquet/json).

## 7) أول إجراءاتك في المحادثة الجديدة
1. فعّل البيئة وأكّد الأساس الأخضر: `powershell -ExecutionPolicy Bypass -File ".\run_validation.ps1"` (يجب 309 اختباراً + ALL GREEN).
2. إن لم تُثبَّت 11.1 بعد، نفّذ:
   ```
   git add -A
   git commit -m "Phase 11.1: smart transparent loader (load/peek) — csv/tsv; +io extra; +shared contract helpers; docs/tests; 309 green, cov 76%"
   ```
3. لا تبدأ تعديلاً قبل الاتفاق على المسار (A أو B). أي تعديل: عبر bash + `py_compile` + عدّ null bytes، ثم `run_validation.ps1`، ثم commit.

## 8) ملفات مرجعية للقراءة
`AUDIT_REPORT.md` ← `LOADER_PHILOSOPHY.md` ← `LOADER_SPEC_11_1.md` ← `ROADMAP.md` (قسم Phase 11) ← `src/dextra/_loader.py` ← `tests/test_phase11.py` ← `CHANGELOG.md`.

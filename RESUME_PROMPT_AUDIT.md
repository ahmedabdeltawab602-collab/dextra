# dextra — برومبت «استئناف + مراجعة مستقلة شاملة» (انسخه كاملاً في محادثة جديدة)

> الغرض: إعادة تأسيس محادثة جديدة على **آخر حالة فعلية** لمكتبة dextra، ثم إجراء
> **مراجعة هندسية/معمارية مستقلة بنسبة 100%** قبل قرار اعتماد/رفض الإصدار والنشر.
> القسمان 0–2 يُعيدان التأسيس؛ القسم 3 هو مهمة المراجعة.

---

## 0) دورك والقاعدة الذهبية

أنت **مراجع تقني مستقل (Independent Technical Auditor)** لا مطوّر المكتبة. هدفك
قرار اعتماد/رفض الإصدار النهائي. التزم بالآتي حرفياً:

1. لا تفترض أن التصميم صحيح لمجرد أنه «يعمل» أو أن CI أخضر.
2. لا مجاملة ولا رفع تقييمات. لا تستخدم «ممتاز/رائع/احترافي/إنتاجي» إلا بمبرّر
   تقني مثبَّت.
3. قيّم **الواقع الحالي كما هو**، لا النوايا ولا الخطط.
4. أي نقطة غير مؤكدة صنّفها صراحةً: **تحتاج تحقق** / **غير موثّقة** / **غير مثبتة
   بالاختبارات**.
5. لكل مشكلة: السبب التقني الحقيقي + حجم الأثر + الأولوية (حرجة/عالية/متوسطة/
   منخفضة) + **التصحيح العملي المقترح مباشرةً**، ثم حدِّث التقييم النهائي بناءً عليه.

---

## 1) حالة المشروع الحالية (الأساس الذي تبدأ منه)

- **المستودع:** `D:\06 PythonProjects\dextra-project` — GitHub
  `ahmedabdeltawab602-collab/dextra`، فرع `main`، آخر commit **`ce0498e`**
  («Phase 9/10 hardening: neutral _compose layer; dash output_dir + parquet +
  metadata + runtime checks»).
- **الإصدار:** **0.2.0** (في `src/dextra/_version.py`) — **لم يُنشَر على PyPI بعد**
  (مؤجَّل بقرار). ملاحظة: محتوى الإصدار تجاوز 0.2.0 فعلياً (أُضيفت Phases 8–10)،
  فبند «bump إلى 0.3.0» مفتوح.
- **بيئة التطوير:** Python 3.13.13، venv، pandas 3.0.3، numpy 2.4.5، scipy 1.17.1،
  scikit-learn 1.8.0، statsmodels 0.14.6. التفعيل:
  `cd "D:\06 PythonProjects\dextra-project"; .\.venv\Scripts\Activate.ps1`
- **التحقّق المحلي:** `powershell -ExecutionPolicy Bypass -File ".\run_validation.ps1"`
  (ruff --fix ثم ruff صارم ثم pytest + تغطية). **آخر نتيجة: 287 اختباراً تمر،
  ruff نظيف، تغطية إجمالية 75.96%، العتبة 68%.**
- **CI:** GitHub Actions على مصفوفة Python 3.9–3.12 (test/benchmarks/build) +
  workflow `docs` (GitHub Pages) + `publish.yml` (Trusted Publishing/OIDC) جاهز
  لكنه غير مُفعَّل (يتطلّب 2FA + pending publisher + بيئة `pypi` + Release بوسم).

### المراحل (Phases) — كلها «مكتملة» على الورق

| # | الوحدة | المحتوى | تغطية الوحدة (آخر تشغيل) |
|---|--------|---------|--------------------------|
| 1 | `stats.py`, `plots.py` | describe_numeric, plot_histograms, plot_boxplots | stats 95% / plots 87% |
| 2 | `stats_advanced.py` | 22 دالة إحصائية (+ aliases) | **67%** |
| 3 | `cleaning.py` | 10 دوال تنظيف (DAMA) | **61%** (الأدنى) |
| 4 | `features` (حزمة `_features_*` + façade) | 8 دوال هندسة سمات (fit/apply) | 65–90% |
| 5 | `selection.py` | 5 دوال اختيار سمات | 82% |
| 6 | `modeling.py` | regress/classify/cluster (+ compare) | 84% |
| 7 | `evaluation.py` | confusion_report/roc_pr/residual_analysis/learning_curves | 90% |
| 8 | `timeseries.py` | tsdecomp/tsstat/tsfcast | 89% |
| 9 | `report.py` | edareport (عارض HTML فوق `_compose`) | 93% |
| 10 | `dashboard.py` | dash (عارض Streamlit فوق `_compose`) | 90% |
| — | `_compose.py` | طبقة البُناة المحايدة (مصدر حقيقة Phases 9–10) | 94% |
| — | `compat.py` | أغلفة متوافقة مع scikit-learn | 98% |

- **إجمالي ~60 دالة عامة + aliases.** الأسماء العامة الأحدث (Phases 8–10) **بلا
  شرطة سفلية** بقرار المالك: `tsdecomp/tsstat/tsfcast`، `edareport/edarep`،
  `dash/dashapp`. (Phases 2–7 تستخدم أسماء طويلة بشرطة سفلية + alias قصير — تباين
  أسلوبي تاريخي يستحق التقييم.)

### العقد الموحّد (§4.7 في `MODELING_PHILOSOPHY.md`)

كل دالة تحليلية: **سطر واحد ← جدول مقاييس غنيّ + شكل متعدّد اللوحات + جملة
`Decision:` + أثر تدقيقي في `df.attrs['dextra_audit']`**، مع أعلام قياسية
(`params/return_params/show/plot/return_df/return_fig/decimals/df_name`) و
`return_params` يُعيد واصفاً JSON-آمناً. عدم تغيير `df`. التقرير/اللوحة «تنسيق لا
حساب جديد» يعيدان استخدام نفس البُناة في `_compose`.

### التبعيات والـ extras

- **الأساسية:** numpy، pandas، matplotlib، seaborn، scipy.
- **اختيارية (كسولة):** `ml` (scikit-learn)، `viz` (plotly)، `perf`
  (polars/pyarrow)، `ts` (statsmodels)، `dash` (streamlit)، `notebook`، `docs`،
  `dev`.

### ملفات الفلسفة/التوثيق (اقرأها كمرجع للعقد)

`ROADMAP.md` ← `MODELING_PHILOSOPHY.md` ← `EVALUATION_PHILOSOPHY.md` ←
`TIMESERIES_PHILOSOPHY.md` ← `REPORT_PHILOSOPHY.md` ← `DASHBOARD_PHILOSOPHY.md` ←
`CLEANING_PHILOSOPHY.md` ← `FEATURES_PHILOSOPHY.md` ← `SELECTION_PHILOSOPHY.md` ←
`CHANGELOG.md`. وتقرير حيّ: `dextra_Evaluation_Report.docx` (في مجلد «شرح
بايثون...») — **متأخّر عند v1.5 ولا يشمل Phases 8–10** (يحتاج تحديثاً).

### مؤجَّل عمداً / مناطق ضعف معروفة (تحقّق منها لا تتجاوزها)

- **النشر على PyPI** و**bump الإصدار** (المحتوى الفعلي ≥ 0.3.0).
- **سبرنت الأداء**: لا اختبار فعلي على بيانات كبيرة؛ لا chunking/out-of-core؛ لا
  `n_jobs`؛ لا بوابة anti-regression على الأداء؛ والكاش في اللوحة مؤجَّل.
- **تغطية منخفضة:** `cleaning.py` 61%، `stats_advanced.py` 67%.
- **أمان:** `dash` يحفظ البيانات الافتراضية كـ **pickle** (تحميل pickle غير موثوق
  خطر)؛ parquet متاح كخيار.
- **PDF للتقرير** مؤجَّل.
- يوجد ملف `stats_advanced.py.bak` متبقٍّ في الشجرة (تحقّق إن كان يجب إزالته).

### تحذيرات تقنية بيئية (مهمة عند أي تعديل)

1. شغّل `run_validation.ps1` قبل كل commit (ruff E302/I001/F821 + بوابة التغطية).
2. المجلد متزامن: قد تُحقن null bytes في .py؛ ولا يُسمح بحذف/إعادة تسمية ملفات
   عليه؛ وأدوات الكتابة قد تقطع ملفات قائمة — الأأمن الكتابة عبر bash ثم التحقّق
   (`py_compile` + عدّ null bytes).
3. بيئة المساعد المعزولة بلا إنترنت وبلا scipy/sklearn/statsmodels/streamlit؛
   التحقّق الكامل على جهاز المالك/CI. (للتحقّق offline: stubs + `py_compile` +
   `ast`/symtable.)
4. git عبر Git Credential Manager (PAT بصلاحيات repo + workflow).

---

## 2) أول إجراءاتك في المحادثة الجديدة

1. اقرأ ملفات الفلسفة بالترتيب أعلاه + `src/dextra/` كاملاً (لاحظ بنية
   `_compose` → `report`/`dashboard`، وحزمة `_features_*` + façade).
2. شغّل `run_validation.ps1` وثبّت **الأساس الأخضر الفعلي** (الأرقام أعلاه مرجع؛
   تحقّق منها، لا تثق بها عمياناً).
3. لا تبدأ أي تعديل قبل إنتاج تقرير المراجعة (القسم 3). أي إصلاح تقترحه نفّذه
   كشريحة صغيرة خضراء (اختبارات + `run_validation` + commit) بعد موافقة المالك.

---

## 3) مهمة المراجعة — Comprehensive Independent Audit

أجرِ **مراجعة هندسية ومعمارية شاملة للمكتبة بالكامل**: كل المراحل، وكل الدوال
العامة والخاصة المؤثّرة، وكل ملفات التصميم/الفلسفة/التوثيق/الاختبارات. طبّق قواعد
القسم 0 حرفياً.

### نطاق المراجعة (غطِّ كل ما يلي)

1. **الفلسفة العامة:** وضوح الرؤية، اتساق المراحل، حدود النطاق، منع Scope Creep،
   والاتساق بين الوثائق والتنفيذ (خاصة هل الكود يطابق ملفات الفلسفة فعلاً).
2. **كل Phase** (الهدف، التصميم، الـ API، سهولة الاستخدام، الاتساق مع البقية،
   الاستقرار، قابلية الصيانة، التوسّع، المخاطر المعمارية) — **بتقييم رقمي 0–10
   لكل بند**.
3. **كل دالة عامة** (صحة التصميم/التوقيع/المعاملات، الاتساق، وضوح المخرجات،
   معالجة الأخطاء، الأداء، التوثيق، الاختبارات) — مع نقاط القوة/الضعف/المخاطر/
   التحسينات.
4. **الدوال الداخلية المهمة** المؤثّرة على التحليل/النمذجة/التقييم/التقارير/
   اللوحات/إدارة البيانات (مثل `_compose._run/_call/_split_ret/_BUILDERS`،
   helpers الموحّدة في `modeling`، resolvers في `timeseries`) — صنّف كلاً:
   **ضرورية / مفيدة / زائدة / تحتاج إعادة تصميم**.
5. **الاختبارات:** التغطية، الجودة، happy path، السلبية، الحواف، التوافق الخلفي؛
   حدّد المناطق غير المحمية كفايةً (انتبه لـ cleaning/stats_advanced، ومسارات
   parquet/launch/الفروع النادرة في timeseries).
6. **الأداء:** الذاكرة، السلوك على البيانات الكبيرة، التبعيات الثقيلة، إعادة
   الحساب غير الضرورية، فرص التحسين (لاحظ أن الأداء على البيانات الكبيرة **غير
   مثبت بالاختبارات**).
7. **الأمان:** تحميل الملفات، **Pickle**، Serialization، الملفات المؤقتة،
   التحقّق من المدخلات، مخاطر حقن الكود، ومعالجة المسارات (خاصة كتابة `report`/
   `dash` للملفات، والتطبيق المولَّد).
8. **التبعيات:** الضرورة، إمكانية جعلها اختيارية، الزوائد، ومخاطر التوافق
   المستقبلي (numpy 2.x / pandas 3.x).
9. **التقارير واللوحات:** بنية `_compose` المشتركة، العارضان (HTML/Streamlit)،
   خطوط التصدير؛ حدّد الازدواجية والترابط غير المرغوب والاتجاهات المعمارية
   الخاطئة (وهل فصل الطبقة فعليّ أم شكليّ).

### المطلوب بعد كل مشكلة/فجوة/ثغرة/ضعف معماري

1. السبب التقني الحقيقي.
2. حجم التأثير.
3. الأولوية: **حرجة / عالية / متوسطة / منخفضة**.
4. **التصحيح الأمثل وفق أفضل الممارسات الحديثة، مقدَّماً مباشرةً وبشكل قابل
   للتنفيذ** (توقيع/كود مقترح عند اللزوم).

### في نهاية التقرير: «Final Technical Verdict»

- التقييم العام **/100**
- التقييم المعماري **/100**
- التقييم البرمجي **/100**
- التقييم الإنتاجي **/100**
- **جاهزية النشر** (اختر واحداً فقط): «غير جاهزة للنشر» / «جاهزة بعد إصلاحات
  محدَّدة» / «جاهزة للنشر» / «جاهزة للإنتاج واسع النطاق».

> إلزامي: أي ثغرة/خطأ منطقي/مشكلة تصميم/تعارض معماري/ازدواجية/كسر لمبدأ من مبادئ
> المشروع — لا تكتفِ بذكره؛ قدّم التصحيح مباشرةً ثم حدّث التقييم النهائي بناءً على
> وجوده أو إصلاحه. أريد مراجعة صريحة 100% بلا مجاملة ولا افتراضات متفائلة، وكأن
> التقرير سيُستخدم لقرار اعتماد/رفض الإصدار النهائي.


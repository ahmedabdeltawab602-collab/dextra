# dextra — تقرير مراجعة هندسية مستقلة شاملة (Independent Technical Audit)

**المستودع:** `ahmedabdeltawab602-collab/dextra` — فرع `main` — آخر commit `ce0498e`
**الإصدار المُعلَن:** 0.2.0 — **غير منشور على PyPI**
**تاريخ المراجعة:** 2026-06-06
**صفة المراجِع:** مدقّق تقني مستقل (لا مطوّر المكتبة). قرار: اعتماد/رفض الإصدار.
**القاعدة:** لا مجاملة، تقييم للواقع كما هو، كل نقطة غير مؤكَّدة مصنَّفة صراحةً.

---

## ملخّص تنفيذي (الحكم أولاً للقارئ المستعجل؛ التفصيل في النهاية)

dextra مكتبة **مبنية بانضباط معماري حقيقي**: طبقة `_compose` المحايدة فصلٌ فعليّ لا
شكليّ، عقد fit/apply الآمن من التسريب مُطبَّق بإحكام في النمذجة، والتبعيات الثقيلة
كسولة بحقّ. لكنها **ليست جاهزة للنشر كما هي** بسبب ثلاث مشكلات إصدارية ملموسة
(عدم تطابق رقم الإصدار، ملف `.bak` يُشحن فعلياً، و pickle كصيغة افتراضية في اللوحة)
وفجوة اتساق في «العقد الموحّد» بين المراحل 1–2 وبقية المكتبة.

**الحكم:** «**جاهزة بعد إصلاحات محدَّدة**» — ليست «جاهزة للنشر» بحالتها، وليست «جاهزة
لإنتاج واسع النطاق» (لا إثبات أداء على بيانات كبيرة).

| المحور | الدرجة |
|---|---|
| التقييم العام | **82 / 100** |
| المعماري | **85 / 100** |
| البرمجي | **80 / 100** |
| الإنتاجي | **66 / 100** |

---

## 1) منهجية المراجعة وحدودها (شفافية إلزامية)

أُجريت المراجعة على **الكود الفعلي في المستودع**، مع تحقّق ثابت مستقل. ما **تحقّقتُ منه
بنفسي** وما **لم أستطع** تشغيله:

**تحقّقتُ منه مباشرةً (لا ثقة عمياء):**
- `py_compile` لكل وحدات `src/dextra/*.py` → **تمر كلها**.
- فحص **null bytes** عبر الملفات → **صفر** (لا تلوّث من المزامنة).
- بنية `_compose`/`report`/`dashboard`/`compat`/`__init__`/`_utils` مقروءة سطراً بسطر.
- مطابقة العقد عبر `grep`/`ast`: حضور الأعلام القياسية، `return_params`، `dextra_audit`،
  `inplace=True`، سطح الأمان (`pickle/eval/exec/subprocess`).
- ملفات الفلسفة الثمانية + ROADMAP + الاختبارات + `pyproject.toml` + كل ملفات `.github/workflows`.

**لم أستطع تشغيله (يبقى على جهاز المالك / CI):**
- `run_validation.ps1` سكربت PowerShell على Windows؛ بيئتي Linux.
- البيئة هنا فيها numpy/pandas/matplotlib/seaborn فقط — **بلا** scipy/sklearn/statsmodels/streamlit،
  وبلا pytest/ruff. لذلك **لم أُعِد تشغيل المجموعة الكاملة ديناميكياً**؛ اعتمدتُ
  `validation_log.txt` كـ«أساس مُبلَّغ» وتحقّقتُ منه ثابتاً.

**تصنيف الثقة:** كل بند أدناه موسوم ضمنياً: ما هو **مثبت** بالفحص الثابت، وما يحتاج
**تحقّقاً ديناميكياً** على جهازك.

---

## 2) الأساس الأخضر (مُتحقَّق من اتساقه، لا من تشغيله)

`validation_log.txt` يُبلِّغ: **287 اختباراً تمر، ruff نظيف، تغطية إجمالية 75.96% (العتبة 68%)**.
تحقّقاتي المستقلة المتوافقة مع ذلك: 233 دالة `test_` فعلية (تتمدّد إلى 287 عبر
`parametrize`)؛ CI يثبّت `[dev,ml]` على Python 3.9–3.12 — أي أن sklearn/statsmodels/scipy/plotly
**حاضرة فعلاً في CI**، فحُرّاس `pytest.importorskip` **لا يُسقِطون الاختبارات صامتين في CI**
(يُسقطونها فقط لدى مستخدم بلا extras، وهذا تصميم مقصود). هذه نقطة إيجابية مهمة:
الـ287 مُمارَسة فعلياً عبر المصفوفة، لا أرقاماً ورقية.

> **تحتاج تحقّقاً:** أعِد تشغيل `run_validation.ps1` على جهازك للتأكيد العددي قبل أي قرار نشر.

---

## 3) الفلسفة العامة والاتساق بين الوثائق والتنفيذ

الرؤية واضحة ومتماسكة: «سطر واحد ← جدول غنيّ + شكل متعدّد اللوحات + جملة Decision».
المراحل العشر مكتملة على الورق، وحدود النطاق منضبطة (لا Scope Creep صارخ: النمذجة
«أساس لا ضبط»، التقرير «تنسيق لا حساب»). الكود يطابق الفلسفة في **جوهر** الادّعاءات
الكبرى (الفصل، الكسل، اللاتسريب) — وهذا غير شائع في مشاريع بهذا الحجم.

**لكن يوجد تعارض جوهري واحد بين الوثيقة والتنفيذ يجب ألّا يُتجاوز:**

العقد الموحّد كما صيغ في برومبت الاستئناف يقول «**كل دالة تحليلية** ← ... + أثر تدقيقي في
`df.attrs['dextra_audit']` + أعلام قياسية تشمل `return_params/params/df_name`». الواقع المُثبَت:

- `stats.py` (Phase 1) و`stats_advanced.py` (Phase 2) — **22+1 دالة، نحو 40% من السطح
  العام** — فيها **صفر** `return_params` و**صفر** `dextra_audit`، وتستخدم `name=` بدل `df_name`،
  وبلا `params=`/`plot`-as-contract. (مثبت بالفحص: `grep -c return_params stats_advanced.py = 0`،
  `grep -c dextra_audit = 0`.)
- اختبار العقد `test_api_contract.py` يفرض مجموعة الأعلام القياسية على **16 دالة فقط**
  (`_FAMILIES` = Phase 4/5/6)، لا على Phases 1/2/3/7/8. أي أن «العقد الموحّد» **مفروض
  جزئياً** فقط، بينما يُسوَّق على أنه شامل.

**الأثر:** ادّعاء العمومية غير دقيق؛ المستخدم الذي يتعلّم `regress(df_name=...)` يتفاجأ بـ
`normality_test(name=...)` وبغياب `return_params` و سجل التدقيق في نصف المكتبة.

(انظر البند 12 — مشكلة #4 — للتصحيح.)

---

## 4) تقييم كل Phase (درجات 0–10)

البنود: **الهدف/الحدود (G)**، **التصميم/الـAPI (D)**، **الاتساق مع البقية (C)**،
**الاستقرار/معالجة الأخطاء (S)**, **قابلية الصيانة (M)**، **التغطية الفعلية (T)**،
**المخاطر المعمارية (R: 10=بلا مخاطر)**.

| Phase | الوحدة | G | D | C | S | M | T | R | **مرجَّح/10** |
|---|---|---|---|---|---|---|---|---|---|
| 1 | stats + plots | 9 | 8 | 6 | 8 | 8 | 9 | 9 | **8.1** |
| 2 | stats_advanced | 9 | 8 | 6 | 8 | 6 | 6 | 8 | **7.3** |
| 3 | cleaning | 8 | 7 | 7 | 7 | 6 | 6 | 7 | **6.9** |
| 4 | features (حزمة + façade) | 9 | 8 | 9 | 8 | 8 | 8 | 8 | **8.3** |
| 5 | selection | 9 | 8 | 9 | 8 | 7 | 8 | 8 | **8.1** |
| 6 | modeling | 10 | 9 | 9 | 9 | 8 | 8 | 9 | **8.9** |
| 7 | evaluation | 9 | 8 | 9 | 8 | 8 | 9 | 8 | **8.4** |
| 8 | timeseries | 9 | 8 | 8 | 8 | 8 | 8 | 8 | **8.1** |
| 9 | report | 9 | 8 | 9 | 9 | 8 | 9 | 8 | **8.6** |
| 10 | dashboard | 9 | 7 | 9 | 8 | 8 | 5 | 6 | **7.4** |
| — | _compose | 10 | 9 | 10 | 9 | 9 | 9 | 9 | **9.3** |
| — | compat | 9 | 8 | 9 | 8 | 9 | 9 | 8 | **8.6** |

**ملاحظات حاسمة لكل خانة منخفضة:**
- **Phase 2 (C=6, M=6, T=6):** تباين الأعلام (`name` بدل `df_name`، لا `return_params`/audit)؛
  ملف ضخم (2941 سطراً) أحادي؛ تغطية 67% (الثانية الأدنى).
- **Phase 3 (M=6, T=6):** أكبر ملف منطقياً (2392 سطراً، 10 دوال، 23 `.copy()`، 5 `except Exception`)؛
  تغطية **61% — الأدنى في المكتبة**؛ تضخّم الأسماء (نحو 33 اسماً عاماً لـ10 دوال).
- **Phase 10 (T=5, R=6):** مسار Streamlit الحقيقي **غير مُختبَر** (stub فقط)؛ `parquet` و`launch`
  غير مُمارَسين في CI؛ pickle افتراضي (خطر أمني، البند 9).

---

## 5) تقييم الدوال العامة (أبرز ما يستحق قرار اعتماد)

السطح ~60 دالة + aliases. الجودة العامة **متينة ومتسقة داخل كل مرحلة**: توقيعات واضحة،
تحقّق مدخلات صريح (مثال `normality_test`: `n<3` يرفع `ValueError`؛ التحقّق من `method`)،
وتتبّع صيغ ممتاز (26 معرّف صيغة `F-Mxx-Lyy-zz` مضمّنة في `stats_advanced` — أداة تدقيق نادرة وقيّمة).

نقاط القوة العرضية: لا `inplace=True` في أي مكان (مثبت) → احترام «عدم تغيير df»؛ aliases
هي **نفس الكائن** (مثبت باختبار الهوية)؛ docstrings على كل العموميات (مفروض باختبار).

أبرز نقاط الضعف على مستوى الدالة (بتصحيحها في البند 12):
- **`bin`** عام يُظلّل `builtins.bin` (`from dextra import bin`). #10.
- **`validate_rules`** يستدعي `df.eval(check)` على سلسلة قاعدة (محرّك python عند `.between`) → سطح
  تنفيذ كود إذا أتت القواعد من مصدر غير موثوق. #12.
- دوال Phase 1/2 تفتقر `return_params`/audit/`df_name`. #4.
- **`get_variable_name`** يعتمد تفتيش الإطارات بعمق ثابت — هشّ (ينكسر عند التغليف، يعيد
  «DataFrame» صامتاً)؛ يؤثّر فقط في العناوين/سجل التدقيق. #11.

---

## 6) الدوال الداخلية المؤثّرة — تصنيف

| الدالة/المجموعة | التصنيف | ملاحظة |
|---|---|---|
| `_compose._run / _call / _split_ret / _extract_decision` | **ضرورية** | جوهر التجريد المحايد؛ تصميم نظيف (تمرير الأعلام المقبولة فقط عبر `inspect.signature`). |
| `_compose._BUILDERS` + `_sec_*` | **ضرورية** | مصدر الحقيقة الوحيد للتقرير واللوحة معاً (فصل حقيقي). |
| `_compose._sec_model` (منطق التوجيه) | **تحتاج إعادة تصميم** | إرشاد `is_numeric_dtype(y) and nunique()>10` غير موثّق ويناقض REPORT_PHILOSOPHY («numeric→regress»). #7. |
| `modeling._resolve_target/_resolve_features/_clean_xy/_effective_cv/_metrics_table/_descriptor/_ret_pack/_require_sklearn` | **ضرورية** | تجزئة ممتازة مطابقة لما وعدت به الفلسفة §4.7؛ معاد استخدامها عبر العائلات الثلاث. |
| `timeseries._resolve_series/_resolve_time/_infer_period/_classical_decompose/_suggest_d/...` | **ضرورية** | resolvers واضحة؛ المسار الكلاسيكي بلا تبعية، وSTL/ADF كسولان. |
| `_now_iso` + `_append_audit` (مكرّرتان في 6 وحدات على الأقل) | **زائدة (تكرار)** | انتهاك DRY صريح؛ تغيير مخطّط التدقيق يلزم تعديل N ملفات. #5. |
| `compat._BaseModelWrapper / _as_frame / _check_sklearn` | **مفيدة** | غلاف sklearn نظيف وكسول. (تحفّظ على `labels_`، #13.) |

---

## 7) الاختبارات — التغطية والجودة

**القوة:** 287 اختباراً تُمارَس فعلياً في CI (لا أرقام ورقية)؛ استخدام منضبط لـ
`importorskip` يجعل المجموعة تعمل بلا extras أيضاً؛ اختبارات عقد API (هوية aliases،
docstrings، الأعلام القياسية على العائلات)، واختبارات مطابقة sklearn (clone/pickle/repr)،
واختبارات خاصية (Hypothesis).

**الفجوات المُثبَتة (لا تتجاوزها):**
1. **مسار Streamlit الحقيقي غير مُختبَر** — العارض يُختبر بـ stub؛ streamlit لا يُثبَّت في أي وظيفة CI.
   تنفيذ التطبيق المولَّد الفعلي صندوق أسود.
2. **`parquet`** (يحتاج pyarrow، غير موجود في `[dev,ml]`) و**`launch=True`** (subprocess) و**تحويل
   polars/pyarrow** في `_ensure_pandas` — **غير مُمارَسة في CI** (لا extra `perf`).
3. **`cleaning.py` 61%** و**`stats_advanced.py` 67%** — أدنى تغطيتين؛ فروع نادرة (مسارات ERROR في
   `validate_rules`، فروع التنظيف الحدّية) غير محمية كفايةً.
4. التغطية الإجمالية 76% تُقاس مع كل التبعيات حاضرة؛ لا تعكس بيئة المستخدم الأساسية.

---

## 8) الأداء — مصنَّف كـ«غير مثبت»

- **لا يوجد إثبات تجريبي على بيانات كبيرة** (مُقرّ به في الخريطة كسبرنت مؤجَّل). لا
  chunking/out-of-core، لا `n_jobs`، لا بوابة anti-regression للأداء، وكاش اللوحة مؤجَّل.
- `_compose._run` يولّد شكلاً ويحوّله Base64 لكل قسم — مقبول للتقرير، لكنه يعيد تنفيذ كامل
  التحليل لكل تبويب في اللوحة عند كل تفاعل Streamlit (لا تخزين مؤقت) → بطء محسوس على
  جداول كبيرة. (مخاطرة أداء معمارية، أولوية متوسطة.)
- استيراد `dextra` يستورد `matplotlib.pyplot` فوراً (عبر `_compose`/`stats`/`plots`) → كلفة
  استيراد وحاجة backend في سياق headless. منخفضة.

> **الخلاصة:** الأداء **غير مثبت بالاختبارات**؛ لا يجوز ادّعاء «إنتاجي واسع النطاق».

---

## 9) الأمان

| البند | الحالة | الخطورة |
|---|---|---|
| **pickle افتراضي في `dash`** (`df.to_pickle` + `pd.read_pickle` في التطبيق المولَّد بلا تحذير) | **ثغرة فعلية** | **عالية** — اللوحة مصمَّمة للمشاركة؛ من يشغّل تطبيقاً + pickle من طرف آخر ينفّذ كوداً عشوائياً (RCE). #9. |
| `df.eval(check)` (محرّك python) على قواعد نصية | سطح حقن | متوسطة/منخفضة — القواعد عادةً من المحلّل نفسه؛ خطر فعلي لو أتت من config/خارج. #12. |
| `subprocess.Popen([... ,"streamlit","run", app_path])` | مقبول | منخفضة — قائمة لا `shell=True`، المسار داخلي. |
| كتابة الملفات (`report`/`dash`) | مقبول | منخفضة — مسارات يحدّدها المستخدم؛ لا تجاوز مسارات ظاهر. |
| تحميل التبعيات الكسول | سليم | — |

---

## 10) التبعيات

التصميم **مثالي تقريباً**: الأساس (numpy/pandas/matplotlib/seaborn/scipy) فقط، والبقية extras
كسولة (`ml/viz/perf/ts/dash/...`)، و`import dextra` يعمل بلا أيٍّ منها (مثبت بنية الاستيراد).
حدود الإصدارات معقولة (`pandas>=1.5`, `numpy>=1.23`).

**مخاطر التوافق المستقبلي (تحتاج رصداً):** بيئة تطويرك pandas 3.0.3 + numpy 2.4.5 بينما الحد
الأدنى المُعلَن `pandas>=1.5`/`numpy>=1.23`. **لا CI على pandas 3.x / numpy 2.x مع حد أدنى
مُثبَّت** — أي أن «يعمل من 1.5 إلى 3.0» **غير مثبت**؛ تغييرات pandas 3.x (Copy-on-Write،
سلوك `df.eval`/`attrs`) قد تكسر سلوكاً غير مُغطّى. أوصِ بإضافة وظيفة CI بأدنى الإصدارات
(`pip install "pandas==1.5.*" "numpy==1.23.*"`).

---

## 11) التقارير واللوحات — هل الفصل حقيقي؟

**نعم، حقيقي لا شكليّ (مثبت بقراءة الكود):** `report.py` و`dashboard.py` كلاهما يستوردان
`_BUILDERS` من `_compose` ويمرّان على نفس الكتل `(subtitle, table, img_b64, decision, note)`؛
لا تحليل مُعاد تنفيذه في أيّ عارض. هذا أفضل قرار معماري في المكتبة.

**عيوب الترابط/الازدواجية المتبقّية:**
- `dashboard.py` يعيد تعريف `_now_iso`/`_append_audit` محلياً بدل استيرادهما من `_compose`
  (كما يفعل `report.py`) → ازدواجية + تباين أسلوبي داخل نفس الطبقة. #5.
- **سجل تدقيق ميّت:** `edareport` و`dash` يلحقان قيد `dextra_audit` بـ`df.copy()` **يُهمَل ولا
  يُعاد** (الدالة تعيد المسار/الـmanifest). أي أن «إلحاق أثر تدقيقي» **بلا أثر ملحوظ** للمستخدم.
  إمّا يُعاد الإطار الموسوم، أو يُدرَج الأثر في الـmanifest، أو يُحذف القيد المضلِّل. #6.

---

## 12) سجلّ المشكلات بالأولوية — مع التصحيح المباشر القابل للتنفيذ

> القاعدة: لكل بند **السبب التقني + الأثر + الأولوية + التصحيح**. حُدِّث التقييم النهائي بناءً على وجودها.

### عالية (تمنع قرار «جاهزة للنشر»)

**#1 — عدم تطابق رقم الإصدار.** `_version.py = 0.2.0` بينما شُحنت Phases 8–10 + سبرنت التصليب
*بعد* bump الـ0.2.0. الأثر: إصدار مضلِّل + يمنع نشراً صحيحاً.
**التصحيح:** ارفع إلى `0.3.0` (أو `0.4.0` إن اعتُبر التصليب كاسراً للسطح)، أضِف قسم CHANGELOG،
ثم وسم Release. سطر واحد: `__version__ = "0.3.0"`.

**#2 — `stats_advanced.py.bak` متتبَّع في git (60KB).** مثبت: `git ls-files` يُظهره؛ مُستثنى من
ruff فلا يُفحَص أبداً، ويُدرَج في sdist/wheel. الأثر: كود ميّت يُشحن للمستخدم + لبس مصدر الحقيقة.
**التصحيح:** `git rm src/dextra/stats_advanced.py.bak && git commit`؛ وأضِف `*.bak` إلى `.gitignore`.

**#3 — pickle صيغة افتراضية في `dash`.** `data_format="pickle"` افتراضاً؛ التطبيق المولَّد ينفّذ
`pd.read_pickle(_DATA)` بلا تحذير. الأثر: RCE عند مشاركة تطبيق + بيانات (وهو غرض اللوحة).
**التصحيح:** اجعل الافتراضي `parquet` (والرجوع إلى `csv` إن غاب المحرّك)؛ أبقِ pickle خياراً
صريحاً مع تحذير أمني مطبوع في رأس التطبيق المولَّد وفي docstring؛ وثّق أن pickle يُشغَّل من مصادر موثوقة فقط.

### متوسطة

**#4 — العقد الموحّد غير شامل فعلياً.** Phases 1–2 (~40% من السطح) بلا `return_params`/audit/`df_name`؛
الاختبار يفرض الأعلام على 16 دالة فقط. الأثر: ادّعاء عمومية غير صحيح + تجربة مستخدم متباينة.
**التصحيح (اختر):** (أ) وسّع Phase 1–2 إلى نفس مجموعة الأعلام + سجل التدقيق وأدرِجها في
`_FAMILIES`؛ أو (ب) وثّق صراحةً «عقداً من طبقتين» (تحليلي وصفي مقابل fit/apply) وأوقف ادّعاء الشمول.
الخيار (أ) أنظف على المدى الطويل لكنه يكسر سطح Phase 2 (`name`→`df_name`).

**#5 — تكرار plumbing العقد (DRY).** `_now_iso`+`_append_audit` مكرّرتان في 6 وحدات؛ أنماط
`_ret_pack`/`_fmt_table`/مخطّط التدقيق معادة لكل وحدة. الأثر: أي تغيير عقد = تعديل N ملفات + خطر انحراف.
**التصحيح:** انقلها إلى `_utils.py` (أو `_contract.py` جديد) واستوردها؛ ابدأ بـ`report` كنموذج (يستورد بالفعل).

**#6 — سجل تدقيق غير ملحوظ في report/dash.** يُلحَق بنسخة تُهمَل. **التصحيح:** أدرِج
`dextra_audit` ضمن الـmanifest المُعاد عند `return_params=True`، أو احذف القيد المضلِّل.

**#7 — توجيه نموذج التقرير بإرشاد غير موثّق.** `nunique()>10` يفصل انحدار/تصنيف صامتاً ويناقض
الفلسفة. الأثر: هدف رقمي بـ≤10 قيم فريدة يُوجَّه تصنيفاً (والعكس). **التصحيح:** اجعل المعيار
معاملاً صريحاً `task=("auto"|"regression"|"classification")` مع توثيق العتبة، أو استنتجه من نوع الهدف فقط كما تقول الفلسفة.

**#8 — فجوات اختبار CI (parquet/streamlit/launch/perf).** **التصحيح:** أضِف وظيفة CI بـ`[dev,ml,perf]`
مع اختبار round-trip لـparquet وpolars→pandas، واختبار دخان streamlit حقيقي خفيف (`pip install streamlit`
+ تنفيذ `_build_dashboard` على stub أقرب للواقع)، أو وثّق هذه المسارات كـ«غير مغطّاة في CI» صراحةً.

**#8b — لا CI بأدنى إصدارات pandas/numpy.** «يعمل 1.5→3.0» غير مثبت. **التصحيح:** وظيفة CI بـ
`pandas==1.5.*`/`numpy==1.23.*` ووظيفة بـ`pandas>=3,<4`.

### منخفضة

**#9 — `bin` العام يُظلّل builtin.** **التصحيح:** وثّق التحذير، أو أضِف اسماً بديلاً (`binize`) واترك `bin` alias.
**#10 — تضخّم aliases (Phase 3: ~33 اسماً لـ10 دوال).** **التصحيح:** قلّص إلى alias واحد قصير لكل دالة؛ علّم الباقي deprecated.
**#11 — `get_variable_name` هشّ.** **التصحيح:** اقبل `df_name` صريحاً دوماً واجعل التفتيش رجعةً أخيرة فقط (هو كذلك جزئياً)؛ لا تعتمد عمقاً ثابتاً.
**#12 — `df.eval` على قواعد.** **التصحيح:** وثّق افتراض الثقة؛ ولمن يريد تشدّداً، قيّد بـ`engine="numexpr"` أو دالة تحقّق صريحة.
**#13 — `DextraClusterer.labels_` عبر NearestCentroid.** قد تخالف تعيينات وقت الملاءمة (عرف sklearn). **التصحيح:** خزّن تسميات fit الفعلية في `labels_`.
**#14 — استيراد matplotlib المبكر.** **التصحيح:** اقبله (core)، أو أجّل استيراد pyplot داخل الدوال إن أردت استيراداً أخفّ.

---

## 13) Final Technical Verdict

استناداً إلى فحص ثابت مستقل للكود الفعلي (تجميع نظيف، صفر null bytes، قراءة معمارية كاملة،
ومطابقة عقد) لا إلى النوايا أو خُضرة CI وحدها:

| المحور | الدرجة | التبرير المختصر |
|---|---|---|
| **التقييم العام** | **82 / 100** | بنية ناضجة ومنضبطة يعيبها انضباطٌ إصداري ناقص وفجوة عقد. |
| **المعماري** | **85 / 100** | `_compose` فصل حقيقي + عقد fit/apply آمن + كسل التبعيات؛ خصمٌ لتكرار plumbing + عقد من طبقتين غير معلن + سجل تدقيق ميّت. |
| **البرمجي** | **80 / 100** | يجمّع نظيفاً، ruff نظيف، تحقّق مدخلات جيد، تتبّع صيغ ممتاز؛ خصمٌ للتكرار، broad excepts، `bin`، تفتيش الأسماء، pickle. |
| **الإنتاجي** | **66 / 100** | المحور الأضعف: عدم تطابق الإصدار، `.bak` يُشحن، pickle افتراضي، أداء غير مثبت، parquet/streamlit/launch غير مختبرة، غير منشور. |

**جاهزية النشر:** **«جاهزة بعد إصلاحات محدَّدة».**

- **ليست «جاهزة للنشر»** بحالتها بسبب #1 (الإصدار) و#2 (`.bak`) و#3 (pickle) — ثلاثتها إصلاحات
  صغيرة محدّدة وقابلة للإنجاز في شريحة خضراء واحدة.
- **ليست «جاهزة لإنتاج واسع النطاق»** قطعاً: الأداء على البيانات الكبيرة **غير مثبت**، ولا
  CI بأدنى/أحدث إصدارات pandas/numpy، ومسار اللوحة الحقيقي غير مُختبَر.

**أصغر مسار للوصول إلى «جاهزة للنشر» (ترتيب مقترح، كل واحدة شريحة خضراء + `run_validation` + commit):**
1. `git rm` لملف `.bak` (#2).
2. bump الإصدار + CHANGELOG (#1).
3. pickle → parquet افتراضياً + تحذير أمني (#3).
4. قرار صريح في العقد: طبقتان معلنتان أو توحيد Phase 1–2 (#4).
5. إدراج/حذف سجل تدقيق report/dash (#6) + توثيق توجيه `_sec_model` (#7).

بعد 1–3 (والأفضل +4..7) يصبح الحكم **«جاهزة للنشر»** على PyPI كإصدار beta صادق (`0.3.0`).
الوصول إلى **«إنتاج واسع النطاق»** يتطلّب سبرنت الأداء + مصفوفة إصدارات التبعيات + تغطية اللوحة الحقيقية.

---

*أُعدّ هذا التقرير كمراجعة مستقلة بلا مجاملة، للاستخدام في قرار اعتماد/رفض الإصدار. كل
رقم وادّعاء فيه إمّا مثبت بفحص ثابت على الكود الفعلي أو موسوم صراحةً بأنه «يحتاج تحقّقاً ديناميكياً» على جهاز المالك/CI.*

